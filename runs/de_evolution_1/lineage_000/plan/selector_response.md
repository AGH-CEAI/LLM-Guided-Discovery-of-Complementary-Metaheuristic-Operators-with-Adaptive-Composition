{"picks": ["_restart", "_detect_stagnation", "_update_step_size"], "reason": "1. _restart is the most impactful candidate: the restart strategy (how to reinitialize mean, sigma, whether to bias toward best, perturbation magnitude, population size increases like IPOP/BIPOP) is highly problem-dependent and a learned/adaptive version can dramatically improve performance by choosing restart strategies conditioned on problem characteristics (dimension, budget remaining, fitness landscape signals). 2. _detect_stagnation is tightly coupled with restart and controls WHEN restarts trigger — the current fixed heuristic thresholds (stag_limit, sigma threshold, condition number threshold) are crude; an adaptive version that learns problem-specific stagnation detection criteria can prevent premature or delayed restarts, yielding significant gains. 3. _update_step_size (CSA) is a core performance-sensitive mechanism in CMA-ES; the step-size adaptation rate and damping factor directly control exploration vs exploitation trade-off. An adaptive version that adjusts c_sigma/d_sigma or uses alternative adaptation rules (e.g., median success rule, or learned damping schedules) based on search progress can improve convergence speed and robustness across different fitness landscapes."}